- name: Bootstrap Python for Ansible (FIRST TIME ONLY)
  hosts: all
  become: true
  gather_facts: false

  tasks:
    - name: Install Python dependencies using raw
      raw: |
        apt update -y
        apt install -y python3 python3-apt python3-six python3-distutils
